# Features

Create one folder per request under this directory by copying `docs/features/_template/` to `docs/features/<feature-slug>/`. Keep the numbering and filenames unchanged so reviews, automation, and humans can find the required artifacts quickly.

